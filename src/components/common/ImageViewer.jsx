import React from 'react';
import { RViewer, RViewerTrigger } from 'react-viewerjs';
export const ImagePreview = props => {
  let 
  return <RViewer
    imageUrls={props.sourceUrl}
    options={options}
         />;
};
